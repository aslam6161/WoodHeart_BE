{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  },

  "//": [
    "The JWT signing key is NOT stored here — appsettings.Development.json is committed.",
    "Set it once per machine with user-secrets:",
    "  dotnet user-secrets set \"Jwt:SigningKey\" \"<at least 32 random characters>\"",
    "Run that from backend/src/WoodHeart.Api. The API refuses to start without it,",
    "deliberately: a weak or shared signing key means anyone can mint an Admin token."
  ]
}
