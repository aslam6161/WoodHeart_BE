<Project Sdk="Microsoft.NET.Sdk">

  <!--
    ============================================================================
    THE CORE OF THE ONION.

    This project must NEVER reference another project, and must NEVER take a
    NuGet dependency. No EF Core, no JSON attributes, no mediator types, no
    DI abstractions. If something here needs the outside world, it belongs in
    WoodHeart.Application as a port instead.

    WoodHeart.ArchitectureTests enforces this — see DomainLayerTests.
    ============================================================================
  -->

  <PropertyGroup>
    <RootNamespace>WoodHeart.Domain</RootNamespace>
  </PropertyGroup>

</Project>
